version: "3.8"

services:
  ztguard-portal:
    build: .
    container_name: ztguard-portal
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - pangolin
    labels:
      - "traefik.enable=false"

networks:
  pangolin:
    external: true
